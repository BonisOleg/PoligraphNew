databases:
  - name: poligraph-db
    databaseName: poligraph
    user: poligraph
    plan: free

services:
  - type: web
    name: poligraph
    runtime: python
    plan: free
    buildCommand: './build.sh'
    startCommand: 'python manage.py migrate && python manage.py create_superuser && python -m gunicorn PolygraphNew.wsgi:application --bind 0.0.0.0:$PORT'
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DJANGO_SETTINGS_MODULE
        value: PolygraphNew.settings.production
      - key: RENDER
        value: true
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: polygraph.website,www.polygraph.website
      - key: DATABASE_URL
        fromDatabase:
          name: poligraph-db
          property: connectionString
      - key: DEBUG
        value: false
      - key: WEB_CONCURRENCY
        value: 4
      - key: DJANGO_SUPERUSER_USERNAME
        value: Poliboss
      - key: DJANGO_SUPERUSER_PASSWORD
        generateValue: true
      - key: DJANGO_SUPERUSER_EMAIL
        value: admin@polygraph.local
    healthCheckPath: /health/
